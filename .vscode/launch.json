{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run http_login.exe",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/http_login.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb",
      "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
      "externalConsole": true,
      "environment": [
        {
          "name": "PATH",
          "value": "C:/msys64/ucrt64/bin;${env:PATH}"
        }
      ],
      "preLaunchTask": "build-http"
    }
  ]
}
